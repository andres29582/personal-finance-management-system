# For a physical LAN smoke test, replace <HOST_LAN_IP> with the developer host's LAN address.
# Do not use localhost from another device: it resolves to that device.
# EXPO_PUBLIC_API_URL=http://<HOST_LAN_IP>:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
